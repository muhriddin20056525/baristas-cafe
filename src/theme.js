import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#c7a17a",
    },
    custom: {
      bgColor: "#f9f5f0",
      darkColor: "#151515",
    },
  },
});

export default theme;
